# =============================================================================
# HSA Tracker Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local
# =============================================================================

# -----------------------------------------------------------------------------
# Convex Configuration
# -----------------------------------------------------------------------------
# These are auto-generated when you run `bunx convex dev` for the first time.
# You can also find them in your Convex dashboard.

# Deployment identifier used by `bunx convex dev`
# Format: dev:your-deployment-name
CONVEX_DEPLOYMENT=dev:your-deployment-name

# Public Convex URL for the React client
# Get this from Convex Dashboard → Settings → URL
VITE_CONVEX_URL=https://your-deployment-name.convex.cloud

# Convex HTTP actions URL (for file serving, etc.)
# Usually the same as VITE_CONVEX_URL but with .site instead of .cloud
VITE_CONVEX_SITE_URL=https://your-deployment-name.convex.site

# -----------------------------------------------------------------------------
# OCR Configuration (Optional)
# -----------------------------------------------------------------------------
# These are used by `bun run ocr:deploy` to deploy the Cloud Run OCR proxy
# and configure Convex environment variables.
#
# If you don't need OCR functionality, you can skip these.

# Shared secret for authenticating Convex → Cloud Run requests
# Auto-generated by deploy script if not set: openssl rand -hex 32
CLOUD_RUN_API_SECRET=

# Google Cloud project ID where Document AI is enabled
GOOGLE_CLOUD_PROJECT_ID=hsatracker

# Document AI region (us, eu, etc.)
GOOGLE_CLOUD_LOCATION=us

# Expense Parser processor ID from Google Cloud Console
# Find at: Console → Document AI → Processors → Your Expense Parser → Processor ID
GOOGLE_CLOUD_EXPENSE_PROCESSOR_ID=

# Cloud Run service URL (set automatically after first deploy)
# Format: https://ocr-proxy-XXXXX-uc.a.run.app
CLOUD_RUN_OCR_URL=
