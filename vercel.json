{
  "installCommand": "bun install",
  "buildCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"master\" ]; then bunx convex deploy && bun run build; else bunx convex deploy --preview \"$VERCEL_GIT_COMMIT_REF\" && bun run build; fi",
  "framework": "vite",
  "outputDirectory": "dist"
}
